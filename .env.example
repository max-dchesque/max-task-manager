# =============================================================================
# MAX Task Manager — Variáveis de Ambiente
# =============================================================================
# Copie este arquivo para .env e preencha com valores reais.
# NUNCA commite o arquivo .env com credenciais reais.
# =============================================================================

# -----------------------------------------------------------------------------
# BANCO DE DADOS (obrigatório)
# Formato: postgresql://USUARIO:SENHA@HOST:PORTA/DATABASE?schema=public
# Exemplo Easypanel: postgresql://postgres:suasenha@nome-do-servico:5432/max_task_manager
# Exemplo local:     postgresql://postgres:postgres@localhost:5432/max_task_manager
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://USUARIO:SENHA@HOST:5432/NOME_DO_BANCO?schema=public"

# -----------------------------------------------------------------------------
# NEXT.JS (opcional — configuração de runtime)
# PORT: porta em que o servidor Next.js escuta (padrão: 3000)
# HOSTNAME: interface de rede que o servidor deve escutar (padrão: 0.0.0.0)
# -----------------------------------------------------------------------------
# PORT=3000
# HOSTNAME=0.0.0.0

# -----------------------------------------------------------------------------
# NEXT.JS — Telemetria (opcional)
# Define como 1 para desabilitar o envio de dados anônimos para a Vercel.
# -----------------------------------------------------------------------------
# NEXT_TELEMETRY_DISABLED=1

# -----------------------------------------------------------------------------
# API PÚBLICA (opcional)
# URL base da API para uso pelo cliente (task-manager.ts)
# Em produção, use a URL pública do seu serviço.
# Se não definido, usa http://localhost:3000 como fallback.
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_TASK_API_URL=https://seu-dominio.com

# -----------------------------------------------------------------------------
# NODE ENV (não altere em produção — definido automaticamente pelo Docker)
# Valores válidos: development | production | test
# -----------------------------------------------------------------------------
# NODE_ENV=production
